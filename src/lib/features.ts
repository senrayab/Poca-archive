/**
 * 화면에 무엇을 드러낼지 정하는 스위치들.
 *
 * 기능을 지우지 않고 감출 때 쓴다. 지우면 되살릴 때 다시 만들어야 하고,
 * 저장해둔 값도 갈 곳이 없어진다. 스위치로 두면 자리만 사라지고 코드와
 * 데이터는 그대로 남는다.
 */

/*
 * 카테고리(분류).
 *
 * 미리 정해두고 고르는 방식인데, 올릴 때마다 어느 것이 맞는지 헷갈리고
 * 나중에 확인하기도 어렵다는 판단으로 감췄다. 대신 메모에 알아볼 수 있는
 * 말을 적는 쪽을 쓴다 — 상세보기에서 분류가 있던 자리에 메모가 나온다.
 *
 * 저장된 분류 값은 그대로 있다. true로 바꾸면 고르는 자리도, 걸러 보는
 * 탭도, 통계도 모두 돌아온다.
 */
export const SHOW_CATEGORY = false
