import { Archive } from './Archive'
import { Grid } from './Grid'
import { Header } from './Header'
import { Nav } from './Nav'
import type { Layout } from '../types'

/** 젖빛 유리 결. 가장자리로 분홍·파랑이 어리고, 길찾기는 아래 알약이다. */
export const classic: Layout = { Header, Archive, Grid, Nav }
