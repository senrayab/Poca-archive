import { Archive } from './Archive'
import { Header } from './Header'
import type { Layout } from '../types'

/** 지금까지 써온 레이아웃. 새 스킨이 제 것을 갖기 전까지 모두 이걸 쓴다. */
export const classic: Layout = { Header, Archive }
