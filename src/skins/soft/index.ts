import { Archive } from './Archive'
import { Header } from './Header'
import { Nav } from './Nav'
import type { Layout } from '../types'

/** 회색 바탕에 흰 판이 떠 있는 결. 아래 알약 탭바가 길을 맡는다. */
export const soft: Layout = { Header, Archive, Nav }
